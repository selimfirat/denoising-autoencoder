# TF Denoising Autoenconder
A neural network that denoises a noisy input. This implementation is done with Tensorflow and transforms an image with gaussian additive noise into denoised image.

![Result](result.png)